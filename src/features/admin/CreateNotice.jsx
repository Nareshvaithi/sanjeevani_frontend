import { useFormik } from "formik";

const CreateNotice = ()=>{
    const formik = useFormik({
        initialValues:{
            notice:''
        }
    })
    return(
        <section>

        </section>
    )
}

export default CreateNotice;